import React, {Component} from 'react';
import {Text, View} from 'react-native';

class User extends Component {
  render() {
    return (
      <View>
        <Text style={{fontSize:30,color:"green"}}>User Class Component</Text>
      </View>
    );
  }
}

export default User;
