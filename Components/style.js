const { StyleSheet } = require("react-native");

export default ExternalStyles = StyleSheet.create({
    textBox: {
      fontSize: 40,
      color: '#fff',
      backgroundColor:"red",
      marginTop:20
    },
  });